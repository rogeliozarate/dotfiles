# My very own dotfiles

## git

### .gitignore

This is a global .gitignore .
Must be placed at :

      ~/.gitignore

And configured with:

      git config --global core.excludesfile '~/.gitignore'

